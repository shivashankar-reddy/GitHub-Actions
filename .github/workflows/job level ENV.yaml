name: job level ENV Example

on:
  workflow_dispatch:
jobs:
  example:
    runs-on: ubuntu-latest
    env:
     company: Siva IT
     name: Siva
    steps:
       - name: first step
         run: echo "company is $company "
       - name: next step
         run: echo "name is $name"

  another:
    runs-on: ubuntu-latest
    steps:
       - name: step
         run: echo "company is $company"
        
