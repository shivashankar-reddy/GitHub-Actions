name: testing the workflow

on: workflow_dispatch

jobs: 
    test-job:
        runs-on: ubuntu-latest
        steps:
            - name: Display date
              run: date
            - name: print Shiva
              run: echo "Shiva"



